<li>
  Name: <%= user.name %>
  <span class="profile-link mx-2">
    <%= link_to 'See Profile',  user_path(user), class: 'profile-link mx-2' %>
    <%= link_to 'Invite', friendships_path(friend_id:user.id), method: :post, class: 'profile-link mx-2'%>
  </span>
 
</li>